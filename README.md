# PI Industries - Sales Playbook

This is a React + Tailwind CSS-based digital version of the Sales Effectiveness Playbook for field teams.

## Setup Instructions

1. Install Node.js
2. Run `npm install`
3. Run `npm run dev` for local preview
4. To deploy, connect to Netlify and use `npm run build` with `dist` folder as publish directory
